annotated-types==0.7.0
anyio==4.12.0
black==25.11.0
boto3==1.42.5
botocore==1.42.5
brotli==1.2.0
certifi==2025.11.12
cffi==2.0.0
charset-normalizer==3.4.4
# `click==8.3.1` is not available on PyPI for your environment. Use a published release.
click==8.1.8
cryptography==46.0.3
cssselect2==0.8.0
# `dnspython==2.8.0` is not available on PyPI for your environment. Use the latest published release.
dnspython==2.7.0
duckdb==1.4.3
ecdsa==0.19.1
email-validator==2.3.0
et_xmlfile==2.0.0
fastapi==0.110.1
flake8==7.3.0
# `fonttools==4.61.1` is not available on PyPI for your environment. Use a published release.
fonttools==4.60.2
h11==0.16.0
idna==3.11
# `iniconfig==2.3.0` is not available on PyPI for your environment. Use a published release.
iniconfig==2.1.0
# `isort==7.0.0` requires Python >=3.10; use a 3.9-compatible release.
isort==6.1.0
Jinja2==3.1.6
jmespath==1.0.1
jq==1.10.0
librt==0.7.3
# `markdown-it-py==4.0.0` requires Python >=3.10; use a 3.9-compatible release.
markdown-it-py==3.0.0
MarkupSafe==3.0.3
mccabe==0.7.0
mdurl==0.1.2
motor==3.3.1
python-multipart
pandas==2.2.3
openpyxl==3.1.5
pytest>=7.0.0
google-genai certifi

# Optional LLM provider dependencies (install only if using that provider)
# openai>=1.0.0  # For OpenAI provider
# google-generativeai>=0.3.0  # For Gemini provider
